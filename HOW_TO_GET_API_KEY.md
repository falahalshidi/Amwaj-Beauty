# كيفية الحصول على Anon Key الصحيح - خطوة بخطوة

## ⚠️ خطأ "Invalid API key" يعني:
- المفتاح غير صحيح
- أو المفتاح من نوع خاطئ (استخدمت service_role بدلاً من anon)
- أو المفتاح من مشروع آخر

## ✅ الحل الصحيح:

### الخطوة 1: افتح Supabase Dashboard

اذهب إلى: **https://supabase.com/dashboard/project/flivraxgfeqrguienahe/settings/api**

### الخطوة 2: ابحث عن "Project API keys"

ستجد قسمين:
- **anon** / **public** key ← هذا ما نحتاجه ✅
- **service_role** key ← هذا للـ backend فقط ❌

### الخطوة 3: انسخ المفتاح الصحيح

1. ابحث عن **"anon"** أو **"public"** key
2. انقر على أيقونة **👁️** أو **"Reveal"** لإظهار المفتاح
3. انقر على أيقونة **📋** أو **"Copy"** لنسخ المفتاح
4. المفتاح طويل جداً ويبدأ عادة بـ `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

### الخطوة 4: الصق المفتاح في ملف `.env`

افتح ملف `.env` واستبدل:
```env
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE
```

بالمفتاح الذي نسخته:
```env
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZsaXZyYXhnZmVxcmd1aWVuYWhlIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MTkwMDAwMDAwMH0.actual_key_here
```

### الخطوة 5: احفظ الملف

**Ctrl+S** لحفظ الملف

### الخطوة 6: أعد تشغيل السيرفر

```bash
# أوقف السيرفر (Ctrl+C)
npm run dev
```

## 🔍 كيف تعرف أن المفتاح صحيح؟

### المفتاح الصحيح (anon):
- ✅ يبدأ بـ `eyJ...`
- ✅ طويل جداً (أكثر من 200 حرف)
- ✅ في قسم "anon" أو "public"
- ✅ يمكن استخدامه في Frontend

### المفتاح الخاطئ (service_role):
- ❌ في قسم "service_role"
- ❌ لا تستخدمه في Frontend!
- ❌ سري جداً - للـ backend فقط

## 📸 صورة توضيحية:

في صفحة API Settings ستجد:

```
Project API keys
├── anon / public
│   └── eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... ← هذا ما نحتاجه ✅
└── service_role
    └── eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... ← لا تستخدمه ❌
```

## ⚠️ أخطاء شائعة:

### 1. نسخت service_role بدلاً من anon
**الحل:** استخدم anon key فقط

### 2. نسخت جزء من المفتاح فقط
**الحل:** انسخ المفتاح كاملاً (طويل جداً)

### 3. أضفت مسافات قبل/بعد المفتاح
**الحل:** تأكد من عدم وجود مسافات:
```env
# ❌ خطأ
VITE_SUPABASE_ANON_KEY= eyJ... 

# ✅ صحيح
VITE_SUPABASE_ANON_KEY=eyJ...
```

### 4. لم تحفظ الملف
**الحل:** احفظ الملف (Ctrl+S) قبل إعادة التشغيل

## ✅ التحقق من النجاح:

بعد إعادة التشغيل:
1. افتح Console (F12)
2. ابحث عن: `✅ Supabase client initialized`
3. جرب تسجيل الدخول
4. إذا لم يظهر خطأ "Invalid API key" = نجح! ✅

## 🆘 إذا استمرت المشكلة:

1. تأكد من أنك نسخت **anon** key وليس **service_role**
2. تأكد من نسخ المفتاح كاملاً
3. تأكد من حفظ ملف `.env`
4. تأكد من إعادة تشغيل السيرفر
5. تحقق من أن URL صحيح: `https://flivraxgfeqrguienahe.supabase.co`

