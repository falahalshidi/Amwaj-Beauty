# تحديث ملف .env - إصلاح الاتصال بـ Supabase

## ✅ تم إصلاح المشكلة!

تم تحديث الكود ليدعم رفع الصور من الألبوم. الآن تحتاج فقط إلى تحديث ملف `.env` بالمفاتيح الصحيحة.

## 📝 خطوات تحديث ملف .env

### 1. افتح ملف `.env` في المجلد الرئيسي

### 2. أضف أو حدّث المحتوى التالي:

```env
VITE_SUPABASE_URL=https://flivraxgfeqrguienahe.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZsaXZyYXhnZmVxcmd1aWVuYWhlIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ2ODY4ODksImV4cCI6MjA4MDI2Mjg4OX0.w3WvowrDngrc6N_zQOIQ8sFBhVq2gkhB01suGiJJNW0
```

### 3. احفظ الملف

### 4. أعد تشغيل السيرفر:

```bash
# أوقف السيرفر (Ctrl+C)
npm run dev
```

## ✅ ما تم إنجازه:

1. ✅ **إنشاء Storage Bucket للصور** - تم إنشاء bucket باسم `product-images` في Supabase
2. ✅ **تعديل نموذج المنتجات** - تم استبدال حقل رابط URL بحقل رفع ملف
3. ✅ **وظيفة رفع الصور** - الصور الآن تُرفع تلقائياً إلى Supabase Storage
4. ✅ **معاينة الصور** - يمكنك رؤية معاينة للصورة قبل الحفظ
5. ✅ **حفظ رابط الصورة** - رابط الصورة يُحفظ تلقائياً في قاعدة البيانات

## 🎯 كيفية استخدام رفع الصور:

1. افتح لوحة التحكم (Admin Panel)
2. اضغط على "إضافة منتج جديد"
3. في حقل "صورة المنتج"، اضغط "Choose File" أو "اختر ملف"
4. اختر صورة من الألبوم
5. ستظهر معاينة للصورة
6. املأ باقي الحقول واضغط "إضافة المنتج"
7. ستُرفع الصورة تلقائياً ويُحفظ رابطها في قاعدة البيانات

## 🔍 التحقق من النجاح:

بعد إعادة تشغيل السيرفر، افتح Console (F12) وابحث عن:
- ✅ `✅ Supabase client initialized` = نجح!
- ❌ `❌ Missing Supabase environment variables!` = تحقق من `.env` مرة أخرى

## 📸 ملاحظات حول الصور:

- الحد الأقصى لحجم الصورة: **5 ميجابايت**
- الصور المدعومة: جميع أنواع الصور (JPG, PNG, GIF, WebP, إلخ)
- الصور تُحفظ في: `product-images/products/` في Supabase Storage
- الصور متاحة للجميع (public access)

## 🐛 إذا استمرت المشكلة:

1. تأكد من حفظ ملف `.env`
2. تأكد من إعادة تشغيل السيرفر
3. افتح Console (F12) وابحث عن الأخطاء
4. تأكد من عدم وجود مسافات قبل/بعد القيم في `.env`

